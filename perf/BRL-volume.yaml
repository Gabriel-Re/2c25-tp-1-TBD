# perf/brl-volume.basic.yaml
config:
  environments:
    api:
      target: "http://localhost:5555"
      plugins:
        statsd:
          host: "localhost"   
          port: 8125
          prefix: "artillery-api"

  pool: 20

  phases:
    - name: Warmup
      duration: 60
      arrivalRate: 10
    - name: Moderate
      duration: 90
      arrivalRate: 50
    - name: Peak
      duration: 60
      arrivalRate: 150
    - name: Cooldown
      duration: 60
      arrivalRate: 20

scenarios:
  - name: Exchange ARS->BRL
    weight: 70
    flow:
      - post:
          url: "/exchange"
          json:
            baseCurrency: "ARS"
            counterCurrency: "BRL"
            baseAccountId: 1
            counterAccountId: 2
            baseAmount: 10000  
          expect:
            - statusCode: 200

  - name: Exchange BRL->ARS
    weight: 30
    flow:
      - post:
          url: "/exchange"
          json:
            baseCurrency: "BRL"
            counterCurrency: "ARS"
            baseAccountId: 1
            counterAccountId: 2
            baseAmount: 100      # Genera volumen desde el lado BRL base
          expect:
            - statusCode: 200

